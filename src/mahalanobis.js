/**
 * Calculates the mahalanobis distance between the point x and a set of points.
 * @param {Array} x
 * @param {Array} points
 */
const distance = (x, points) => {
  const cov = []; // covariance matrix of the points
  // @todo
  return 0;
};

export default distance;
